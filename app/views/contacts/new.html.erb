<div align="center">
  <h1>Send Us a Message</h1>
</div>
  <%= simple_form_for @contact, :html => {:class => 'form-horizontal' } do |f| %>
    <%= f.input :name, :required => true, maxlength: 100 %>
    <%= f.input :email, :required => false, maxlength: 100, placeholder: "Your Email is not required, but we won't be able to reply to you if you chose not to provide it." %>
    <%= f.input :message, :as => :text, :required => true, maxlength: 500 %>
    <div>
      </br>
      <%= f.button :submit, 'Send message', :class=> "btn btn-primary" %>
    </div>
  <% end %>
<p>We ask that anyone who contacts us, that would like a response, submit a secure and private email so that we can respond. This email will not be stored for later use.</p>

